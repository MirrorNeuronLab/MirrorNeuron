defmodule TestPattern do
  def run do
    result = {:error, "job ddl-1c50c86d is not running in the connected cluster"}
    case result do
      {:error, "job " <> _ = reason} -> IO.puts("Matched: #{reason}")
      _ -> IO.puts("No match")
    end
  end
end

TestPattern.run()
