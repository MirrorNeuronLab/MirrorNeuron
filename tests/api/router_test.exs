defmodule MirrorNeuron.API.RouterTest do
  use ExUnit.Case, async: true

  @tag :skip
  test "legacy Plug router tests are disabled until the Elixir HTTP API is restored" do
    assert true
  end
end
